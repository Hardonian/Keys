# Keys Workspace Configuration
#
# src/      - Primary CLI (local-first, no backend required)
# frontend/ - Web UI (deployable to Vercel, Supabase optional)
# backend/  - Server API (optional, requires Supabase)

packages:
  - "src"
  - "frontend"
  - "backend"
